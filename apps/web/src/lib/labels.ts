/**
 * Anzeigetexte für Backend-Enum-Werte. Die Quelle liegt jetzt in @soziolog/shared
 * (damit API-PDF-Export und Frontend identische Bezeichnungen nutzen); hier nur
 * re-exportiert, damit die bestehenden Frontend-Importpfade stabil bleiben.
 */
export {
  GOVERNANCE_LABEL,
  VORSCHLAG_STATUS_LABEL,
  GUELTIGKEIT_STATUS_LABEL,
  BEFRISTUNG_LABEL,
  SCHWEREGRAD_LABEL,
  DOMAENE_TYP_LABEL,
} from '@soziolog/shared';
